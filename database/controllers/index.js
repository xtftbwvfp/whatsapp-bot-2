const users = require("./users")
const messages = require("./messages")
const message_recipients = require("./message_recipients")
const groups = require("./groups")
const users_groups = require("./users_groups")

module.exports = {
  users,
  messages,
  groups,
  message_recipients,
  users_groups,
}
